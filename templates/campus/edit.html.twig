{% extends 'base.html.twig' %}

{% block title %}Edit Campus{% endblock %}

{% block main %}
<br>
<div class="container">
    {{ form_start(form) }}
    <div class="entry-title" style="color: #0d5470">
        <h1>Modifier un campus</h1>
    </div>
    <br><br>
    <div class="row g-3 aligncenter">
        <div class="col">
            {{ form_row(form.nom, {'attr': {'placeholder': "Nom de campus"}}) }}
        </div>
    </div>

    {{ include('campus/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('campus_index') }}">back</a>

    {{ include('campus/_delete_form.html.twig') }}
{% endblock %}
