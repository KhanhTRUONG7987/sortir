{% extends 'base.html.twig' %}

{% block title %}Campus index{% endblock %}

{% block main %}
    <br>
    <div class="container">
        <h1>Gérer les campus</h1>
        <br>
        <div>
            <h4>Filtrer les sites</h4>
            <form id="form_search" action="{{ path('search') }}">
                <a>Le nom contient: </a>
                <label>
                    <input type="text" class="form-control" placeholder="Search" name="find">
                </label>
                <br>
                <button type="submit">Rechercher</button>
            </form>
        </div>
        <table class="table">
            <thead>
            <tr>
                <th>Campus</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {#{% for campus in campuses %}#}
                <tr>
                    {{ form_start(createCampusForm) }}
                    <div class="row g-3">
                        <div class="col">
                            {{ form_row(createCampusForm.nom) }}
                        </div>
                        <div class="col">
                            {{ form_row(createCampusForm.Ajouter) }}
                        </div>
                    </div>
                    {{ form_end(createCampusForm) }}
                </tr>
            {#{% else %}#}
            {#{% endfor %}#}
            <tr>

            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}


{#<td>#}
{#    <label>#}
{#        <input type="text" class="form-control" placeholder="Nom de campus" name="nom_new">#}
{#    </label>#}
{#</td>#}
{#<td>#}
{#    <a href="{{ path('campus_new') }}">Ajouter</a>#}
{#</td>#}